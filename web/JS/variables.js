 // Using var
        var name = "Ayush";

        // Using let
        let age = 21;

        // Using const
        const pi = 3.14;
        console.log(name + " is " + age + " years old. Pi is approximately " + pi);
        // Another Example

        var firstName = "Ayush";
        let lastName = "Narware";
        const birthYear = 2004;
        console.log(firstName + " " + lastName + " was born in " + birthYear);

        //KodNest Example-
        let number1 = 10;
        let number2 = 20;
        let sum = number1 + number2;

        console.log("The sum is: " + sum);

        let score = 50;
        console.log("Initial score:", score);

        score = 75; // reassigning a new value
        console.log("Updated score:", score);

        const maxScore = 100;
        console.log("Max score is:", maxScore);
        maxScore = 150; // This will cause an error because const variables cannot be reassigned
        console.log("Updated max score is:", maxScore);